{
  "name": "gittree",
  "private": true,
  "version": "1.0.0",
  "description": "Git history as a living tree",
  "type": "module",
  "scripts": {
    "build": "pnpm --filter @gittree/core build && pnpm --filter @gittree/web build && pnpm --filter @gittree/site build",
    "dev": "pnpm --filter @gittree/site dev",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=20"
  }
}